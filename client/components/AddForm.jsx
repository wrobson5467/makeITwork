import React from "react";
import { useForm } from "react-hook-form";



function Form(props) {
  // const { register, handleSubmit, watch, formState: { errors } } = useForm();

  return(
  //   <form onSubmit={handleSubmit(onSubmit)}>
  //   {/* register your input into the hook by invoking the "register" function */}
  //   <input defaultValue="test" {...register("Company")} />
    
  //   {/* include validation with required or other standard HTML validation rules */}
  //   <input {...register("Copmany-Type", { required: true })} />
  //   {/* errors will return when field validation fails  */}
  //   {errors.exampleRequired && <span>This field is required</span>}
    
  //   <input {...register("Position", { required: true })} />
  //   <input {...register("Location", { required: true })} />
  //   <input {...register("Remote", { required: true })} />
  //   <input {...register("Job-description", { required: true })} />
  //   <input {...register("Tech Stack", { required: true })} />
  //   <input {...register("Experience level", { required: true })} />
  //   <input {...register("Stage", { required: true })} />
  //   <input {...register("current tasks", { required: true })} />
  //   <input {...register("contacts", { required: true })} />
    
  //   <input type="submit" />
  // </form>
  <text> You are in the modal</text>
  )

}


export default Form;